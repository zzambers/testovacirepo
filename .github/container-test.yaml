name: "Container-test"

on:
  push:
    branches:
      - "test-container"

jobs:
  test-container:
    name: Test container
    runs-on: "ubuntu-latest"
    container: centos:6
    steps:
      - name: Check system
        run: |
          cat /etc/redhat-release
          id
